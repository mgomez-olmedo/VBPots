/**
 * Package with classes focused on performing mapping
 * operations for combination (between result and and
 * operands; res = pot1 x pot2) and marginalization
 * (between result and operand; res = sum_x pot(..x..))
 */
package object mapper {

}
